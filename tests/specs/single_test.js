const assert = require('assert');

describe('Google Search Function', () => {
  it('can find search results', () => {
    browser.url('https://www.terra-institute.eu/en/cookies/#:~:text=A%20cookie%20is%20a%20small,visitors%20access%20to%20various%20functions.');


   /*  browser.url('https://www.vidio.com/watch/1940391-happy-mbb-no-copyright-music-drm');
    var i;
  for (i = 0; i <20; i++) {
    const input = $('[name="q"]');
    input.setValue('aaa');
    } */
    

   /*  const input = $('[name="q"]');
    input.setValue('test123');

    const title = browser.getTitle();
    assert.equal(title, 'Google');*/
  });
  
});
